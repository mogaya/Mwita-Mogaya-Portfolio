import nur from "../assets/nur.png";
import leedsons from "../assets/leedsons.png";
import tectally from "../assets/tectally.png";

const projectImgMap: Record<string, string> = {
  nur: nur,
  leedsons: leedsons,
  tectally: tectally,
};

export default projectImgMap;
